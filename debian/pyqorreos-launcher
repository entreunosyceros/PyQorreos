#!/bin/sh
# Lanzador del paquete Debian: run_app.py crea .venv en el equipo destino.
set -e
ROOT=/usr/share/pyqorreos
RUN_APP="$ROOT/run_app.py"

if [ ! -f "$RUN_APP" ]; then
    echo "PyQorreos: instalación incompleta en $ROOT" >&2
    exit 1
fi

exec python3 "$RUN_APP" "$@"
