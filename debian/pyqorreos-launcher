#!/bin/sh
# Lanzador del paquete Debian (venv en /usr/lib/pyqorreos).
set -e
ROOT=/usr/lib/pyqorreos
PY="$ROOT/.venv/bin/python3"
MAIN="$ROOT/main.py"

if [ ! -x "$PY" ] || [ ! -f "$MAIN" ]; then
    echo "PyQorreos: instalación incompleta en $ROOT" >&2
    exit 1
fi

exec "$PY" "$MAIN" "$@"
